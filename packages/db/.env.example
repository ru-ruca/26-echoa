# 개발 — 공용 docker 컨테이너 local-pgvector-18 의 echoa_db
# 기동: cd ../../../dev-env/common-docker && docker compose up -d
DATABASE_URL=postgresql://user1:userpw!!@localhost:5432/echoa_db

# 프로덕션 — Neon (pooler 엔드포인트). 리전은 sin1/icn1 권장 (20 §6)
# DATABASE_URL=postgresql://user:password@ep-xxx-pooler.ap-southeast-1.aws.neon.tech/echoa?sslmode=require
