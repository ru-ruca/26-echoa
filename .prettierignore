# 마크다운은 전부 손으로 쓴 한국어다. prettier 의 표 정렬은 문자 수로 계산해서
# 한글(표시 폭 2)에서는 오히려 어긋나고, diff 만 커진다.
*.md
**/*.md

# 콘텐츠 파이프라인은 Python(uv) 이라 pnpm 워크스페이스 밖이다
tools/

# 생성물
pnpm-lock.yaml
packages/db/drizzle/
.next/
.turbo/

# Next.js 가 next dev/build 마다 다시 쓰는 파일
apps/web/next-env.d.ts
